server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/b1uebank/UserManagementServiceRegistry
          clone-on-start: true